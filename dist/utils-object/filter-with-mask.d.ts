import { AnyObject } from "@banquette/utils-type/types";
/**
 * Remove all the keys from {obj} that are not defined in {mask};
 */
export declare function filterWithMask(obj: AnyObject, mask: AnyObject): AnyObject;
