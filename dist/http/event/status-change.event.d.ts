import { RequestProgressEvent } from "./request-progress.event";
/**
 * Separate type of event so it's easier to distinguish on the user side.
 */
export declare class StatusChangeEvent extends RequestProgressEvent {
}
