
export interface I18nInterface {
    /**
     * Text of the popover to show when hovering the "align left" button.
     * An empty string will disable the popover.
     */
    alignLeftPopover: string;

    /**
     * Text of the popover to show when hovering the "align center" button.
     * An empty string will disable the popover.
     */
    alignCenterPopover: string;

    /**
     * Text of the popover to show when hovering the "align right" button.
     * An empty string will disable the popover.
     */
    alignRightPopover: string;

    /**
     * Text of the popover to show when hovering the "align justify" button.
     * An empty string will disable the popover.
     */
    alignJustifyPopover: string;
}
