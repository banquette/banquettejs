
export interface I18nInterface {
    /**
     * Text of the popover to show when hovering the "undo" button.
     * An empty string will disable the popover.
     */
    undoPopover: string;

    /**
     * Text of the popover to show when hovering the "redo" button.
     * An empty string will disable the popover.
     */
    redoPopover: string;
}
