
import { VueBuilder } from "@banquette/vue-typescript";
import { default as IconMaterialBadge } from './badge.component.vue';

VueBuilder.RegisterComponent('i-material-badge', IconMaterialBadge);

export { IconMaterialBadge }

